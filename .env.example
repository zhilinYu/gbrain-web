# GBrain MCP Server URL
NEXT_PUBLIC_GBRAIN_URL=http://localhost:8787
GBRAIN_URL=http://localhost:8787

# GBrain API Token（兜底 token，前端未设置时使用）
# 获取方式：gbrain auth create "gbrain-web" --takes-holders world
GBRAIN_TOKEN=
